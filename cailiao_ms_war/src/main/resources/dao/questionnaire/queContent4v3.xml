<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whut.cailiao.war.dao.questionnaire.QueContent4V3Dao">
	<sql id="base_query">
		id,corName,productionLine,questionnaireId,year,tons,clinkerPart,fuelPart,cementStonePart,energyConsumptionPart,exhaustEmissionPart,majorEquipmentPart
	</sql>

	<!-- select -->
	<select id="findQueContent4V3" parameterType="java.lang.Integer"
		resultType="QueContent4V3">
		select
		<include refid="base_query" />
		from
		ms_tb_queContent4V3
		where
		id=#{id}
	</select>

	<!-- delete -->
	<delete id="deleteQueContent4V3" parameterType="java.lang.Integer">
		delete from
		ms_tb_queContent4V3
		where id=#{id}
	</delete>

	<!-- save -->
	<insert id="saveQueContent4V3" parameterType="java.util.Map">
		insert into
		ms_tb_queContent4V3(
		questionnaireId,
		corName,
		productionLine,
		year,
		tons,
		clinkerPart,
		fuelPart,
		cementStonePart,
		energyConsumptionPart,
		exhaustEmissionPart,
		majorEquipmentPart
		)
		values(
		#{questionnaireId},
		#{corName},
		#{produtionLine},
		#{year},
		#{tons},
		#{clinkerPart},
		#{fuelPart},
		#{cementStonePart},
		#{energyConsumptionPart},
		#{exhaustEmissionPart},
		#{majorEquipmentPart}
		)
	</insert>

	<!-- update -->
	<update id="updateQueContent4V3" parameterType="java.util.Map">
		update ms_tb_queContent4V3
		<set>
			questionnaireId=#{questionnaireId}
			<if test="corName!=null">
				corName=#{corName}
			</if>
			<if test="productionLine!=null">
				productionLine=#{productionLine}
			</if>
			<if test="year!=null">
				year=#{year}
			</if>
			<if test="tons!=null">
				tons=#{tons}
			</if>
			<if test="clinkerPart!=null">
				clinkerPart=#{clinkerPart}
			</if>
			<if test="fuelPart!=null">
				fuelPart=#{fuelPart}
			</if>
			<if test="cementStonePart!=null">
				cementStonePart=#{cementStonePart}
			</if>
			<if test="energyConsumptionPart!=null">
				energyConsumptionPart=#{energyConsumptionPart}
			</if>
			<if test="exhaustEmissionPart!=null">
				exhaustEmissionPart=#{exhaustEmissionPart}
			</if>
			<if test="majorEquipmentPart!null">
				majorEquipmentPart=#{majorEquipmentPart}
			</if>
		</set>
	</update>
</mapper>