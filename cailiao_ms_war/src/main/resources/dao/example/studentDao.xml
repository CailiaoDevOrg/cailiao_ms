<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whut.cailiao.dao.example.StudentDao">

    <select id="selectItemByName" resultType="Student" parameterType="java.lang.String">
        SELECT * From ms_tb_student ts WHERE ts.name = #{name}
    </select>

    <insert id="insertNewItem" parameterType="java.lang.String">
        INSERT INTO ms_tb_student (name, age)
    </insert>

</mapper>